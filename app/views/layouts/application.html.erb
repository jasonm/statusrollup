<!DOCTYPE html>
<html>
<head>
  <title>StatusRollup</title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= csrf_meta_tags %>
  <link rel="apple-touch-icon" href="/touch-icon/touch-114.png" />
  <link rel="apple-touch-icon" href="/touch-icon/touch-144.png" sizes="72x72"/>
  <link rel="apple-touch-icon" href="/touch-icon/touch-114.png" sizes="114x114" />
  <link rel="apple-touch-icon" href="/touch-icon/touch-144.png" sizes="144x144" />
</head>
<body class="<%= body_class %>">

<% unless @suppress_navbar %>
  <%= render partial: 'shared/navbar' %>
<% end %>

<div class="container">
  <%= render partial: 'shared/flashes' %>
  <%= render partial: 'shared/announcements' %>

  <%= yield %>

  <footer class="row">
    <ul class="footer-links">
      <li class="block">&copy; Jason Morrison 2013.  All Rights Reserved.</li>
      <li class="block">StatusRollup is a trademark of Jason Morrison.</li>
      <li><a href="https://github.com/jasonm/statusrollup">Source Code and Issues</a></li>
      <span class="divider">&bullet;</span>
      <%= nav_link_to 'Privacy Policy', page_path('privacy') %>
      <span class="divider">&bullet;</span>
      <%= nav_link_to 'Terms of Service', page_path('terms') %>
      <% if signed_in? %>
        <span class="divider">&bullet;</span>
        <%= nav_link_to 'Sign Out', sign_out_path %>
      <% end %>
    </ul>
    </div>
  </footer>
</div>

<%= javascript_include_tag "application" %>
<%= yield :javascript %>

</body>
</html>
