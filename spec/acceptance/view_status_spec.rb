require 'spec_helper'

feature 'Viewing commit statuses for a SHA' do
  scenario 'shows aggregate status'
  scenario 'shows per-service status with details'
  scenario 'shows historical status with details'
end
